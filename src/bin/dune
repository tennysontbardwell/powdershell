(executables
 (names main)
 (promote
  (until-clean)
  (into ../))
 (flags (:standard -cclib -static -cclib -no-pie))
 (libraries yojson oUnit str lambda-term))

(install
 (section bin)
 (files (main.exe as powdershell)))
